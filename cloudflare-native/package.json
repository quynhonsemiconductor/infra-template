{
  "name": "__PRODUCT__",
  "description": "__PRODUCT__ — Cloudflare-native app (Pages + Functions).",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/quynhonsemiconductor/__PRODUCT__.git"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "dev": "wrangler pages dev dist --local",
    "build": "echo \"TODO: your framework build → dist/ (e.g. astro build / vite build)\" && mkdir -p dist",
    "deploy": "wrangler pages deploy dist --project-name __PRODUCT__",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.42.0"
  }
}
